[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "python312", "python312Packages.pip", "python312Packages.setuptools"]

[phases.install]
cmds = [
  "npm i",
  "python3 -m pip install --user -r requirements.txt"
]

[phases.build]
cmds = [
  "npm run build"
]
